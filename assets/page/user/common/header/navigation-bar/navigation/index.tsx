import React from "react";

import {navigations} from "model"

const Navigation = () => {
	return (
		<div>
			this is navigation {navigations.mainNavigations.length}
		</div>
	);
};

export default Navigation;